import styled from "@emotion/styled";
import { themeColors } from "../../../theme/colors";

export const RedirectText = styled.span`
  color: ${themeColors.secondary.expandedGreen};
  text-decoration: underline;
  cursor: pointer;
  padding: 0 0.3rem;
`;
